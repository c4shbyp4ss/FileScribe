#ifndef STARTINGLIST_H
#define STARTINGLIST_H

#include "config/config.h"

/* Zera todas as posições do array, deixando a lista pronta para uso. */
void startingList(char array[][2][MAX_STR_LEN], int tma);

#endif
